 - What is JSX, and why is it used?
 A: jsx stands for javascript xml.it is an extension for javascript, used with react to write cleaner ui.
 - What is the difference between State and Props?
 A: State is a react hook that stores and updates data for a component but props are properties that are passed from parent to child.
 - What is the useState hook, and how does it work?
 A: This hook helps to hold and update data betweem re-rendering of the component. 
 - How can you share state between components in React?
 A: declaring the state in a parent component makes it easier to share between child components.
 - How is event handling done in React?
 A: by using event handler:)
